---
title: nodepad-- 使用
lang: en-US
---
## 替换
```shell
1、打开 Notepad++ 并确保你的文本文件已经打开。
2、使用快捷键 Ctrl + H 打开“搜索”窗口。
3、勾选“正则表达式” (Regular expression) 选项。
4、替换 (Find what) 输入框中留空（或者输入 ^ 表示行首 或 $ 表示行尾）。
5、替换为 (Replace with) 输入框中输入。
6、点击“替换全部” (Replace All) 按钮。

解释：
^ 表示行首。
$ 表示行尾。
```